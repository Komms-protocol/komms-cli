[package]
name = "protocol"
version = "0.2.0"
edition = "2024"
description = "KOMMS chain payload (v0, v1.0, v1.1); vendored for komms-cli"

[dependencies]
ciborium = "0.2.2"
sha2 = "0.10.8"
thiserror = "2.0.12"
